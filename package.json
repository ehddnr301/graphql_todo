{
  "name": "graphql-todo",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^2.0.4",
    "typescript": "^3.9.6"
  },
  "scripts": {
    "start:dev": "nodemon",
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node build/index.js"
  },
  "dependencies": {
    "@types/node": "^14.0.14",
    "rimraf": "^3.0.2",
    "ts-node": "^8.10.2"
  }
}
